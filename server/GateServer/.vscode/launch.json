{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug gateserver",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/build/gateserver",
      "cwd": "${workspaceFolder}",
      "args": [],
    }
  ]
}
